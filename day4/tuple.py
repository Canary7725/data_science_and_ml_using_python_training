
# Tuple are immutable, ordered and they allow duplicates

point = 4, 7, 8  # Packing

x, y, z = point  # Unpacking


print(point.count(4))  # Count the number os apperance of a given value
print(point.index(7))  # Returns index of a given value

print(x)
print(point)
